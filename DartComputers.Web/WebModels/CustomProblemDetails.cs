using Microsoft.AspNetCore.Mvc;

namespace DartComputers.Web.WebModels
{
    public class CustomProblemDetails : ProblemDetails
    {
        public IDictionary<string, string[]> Errors { get; set; } = new Dictionary<string, string[]>();
    }
}
